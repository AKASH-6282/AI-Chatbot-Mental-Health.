# AI Chatbot for Mental Health

This is a prototype chatbot providing non-clinical emotional support using:

- **Flask API** for backend
- **Streamlit** for frontend
- **Hugging Face Transformers** for:
  - Emotion classification (`fine_tuned_model`)
  - Chat generation (`gpt2`)

## How to Run

1. Start Flask API:

```bash
python app.py
